{% extends "_layout.twig" %}

{% set title = "colours" %}
{% set slug = "colours" %}

{% block content %}

  <div class="bg-tertiary--light">
    <section class="site-container  pad">

      <h2 data-bk-layout="" class="font-xl">Colours</h2>

      <div data-bk-layout="lap:7" class="rte">
        <p class="font-m">Colour values are almost always based on brand guidelines or style guides provided by a designer or client.</p>

        <p>Primary and Secondary colour groups are often used for headings, links, button and background colours. Links and buttons may default to the primary colour with the secondary colour used on hover.</p>

        <p>Often a tertiary colour option won't be provided, in these cases it is advised to find a appropriatly fitting grey combination. Tertiary colours are useful for active link states, buttons, and background colours.</p>

        <p>Colours are set in <span class="alert alert--inline alert--simple br-2">/css/settings/_colors.scss</span></p>
      </div>

    </section>
  </div>


  <div class="bg-white">
    <section class="site-container  pad">

      <header data-bk-layout="lap:8" class="rte">
        <h3 class="font-l">Brand Colours</h3>
      </header>

      {% for colour in ['primary', 'secondary', 'tertiary'] %}
        <div data-bk-layout="lap:6">
          <h4 class="font-m color-{{ colour }}">{{ colour|capitalize }}</h4>

          <ol class="font-s">
            <li data-bk-layout="4" class="bg-{{ colour }} pad-t">
              <span style="visibility:hidden">default</span>
            </li>
            <li data-bk-layout="4" class="bg-{{ colour }}--dark color-white pad-t">
              --dark
            </li>
            <li data-bk-layout="4" class="bg-{{ colour }}--light color-black pad-t">
              --light
            </li>
          </ol>

<pre><code data-bk-layout class="alert alert--simple br-0">// Sass
.container {
  background-color: ${{ colour }};
}

&lt;!-- html --&gt;
&lt;div class=&quot;bg-{{ colour }}&quot;&gt;&hellip;&lt;/div&gt;
&lt;h3 class=&quot;color-{{ colour }}&quot;&gt;&hellip;&lt;/h3&gt;</code></pre>
        </div>
      {% endfor %}
    </section>
  </div>

  <div class="bg-tertiary">
    <section class="site-container  pad">

      <header data-bk-layout="lap:8" class="rte">
        <h3 class="font-l">Standard Colours</h3>
      </header>

      {% for colour in ['white', 'black'] %}
        <div data-bk-layout="lap:6">
          <h4 class="font-m color-{{ colour }}">{{ colour|capitalize }}</h4>

          <ol class="font-s">
            <li data-bk-layout="4" class="bg-{{ colour }} pad-t">
              <span style="visibility:hidden">default</span>
            </li>
          </ol>

<pre><code data-bk-layout class="alert alert--simple br-0">// Sass
.container {
  background-color: ${{ colour }};
}

&lt;!-- html --&gt;
&lt;div class=&quot;bg-{{ colour }}&quot;&gt;&hellip;&lt;/div&gt;
&lt;h3 class=&quot;color-{{ colour }}&quot;&gt;&hellip;&lt;/h3&gt;</code></pre>
        </div>
      {% endfor %}
    </section>
  </div>

{% endblock %}