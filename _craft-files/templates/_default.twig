{% if entry is defined %}

  {% extends '_layouts/_main' %}

  {% block content %}

    <div class="site-container pad" id="content">
      <h2 class="font-xl">{{ entry.title }}</h2>
    </div>

  {% endblock %}

{% else %}

  {% redirect "home" %}

{% endif %}